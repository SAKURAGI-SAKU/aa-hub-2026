{
  "name": "aa-gallery-app",
  "version": "1.0.0",
  "description": "AA Exhibition with Auth and Likes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx prisma generate && npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.17.3"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
